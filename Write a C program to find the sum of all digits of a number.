10.// to find the sum of all digits of a number.
#include <stdio.h>

int main()
{
    int num,sum,c;
    printf("Enter the number:");//6
    scanf("%d",&num);
    sum=0;
    while(num!=0){
        c=num%10;
        sum=c+sum;
        num=num/10;
    }
    printf("%d",sum);
    

    return 0;
}
