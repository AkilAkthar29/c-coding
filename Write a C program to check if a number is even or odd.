9.// to check if a number is even or odd
#include <stdio.h>

int main()
{
    int a;
    printf("Enter the number:");
    scanf("%d",&a );
    if(a%2==0){
        printf("even");
    }
    else{
        printf("odd");
    }

    return 0;
}
