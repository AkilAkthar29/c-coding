#include <stdio.h>

// Define a union
union Data {
    int i;
    float f;
    char c;
};

int main() {
    // Declare a variable of type union Data
    union Data data;

    // Print the size of the union
    printf("Size of the union: %lu bytes\n", sizeof(data));

    return 0;
}
